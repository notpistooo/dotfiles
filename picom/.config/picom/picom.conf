shadow = false

fading = false

corner-radius = 10


animations = (
    {
        triggers = ["close", "hide"];
        preset = "disappear";
       	duration = 0.1;
    },
    {
        triggers = ["open", "show"];
        preset = "appear";
       	duration = 0.1;
    },
)

backend = "glx"
vsync = true;
